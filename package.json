{
  "private": true,
  "workspaces": [
    "backend/*",
    "packages/*"
  ],
  "scripts": {
    "test": "lerna run test",
    "new-version": "lerna version --conventional-commits --yes",
    "diff": "lerna diff"
  },
  "devDependencies": {
    "lerna": "^3.13.1"
  },
  "name": "js-fws"
}
